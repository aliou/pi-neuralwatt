---
"@aliou/pi-neuralwatt": patch
---

Add `glm-5.3` to the offline fallback model table to match the live Neuralwatt API: GLM-5.2 pricing parity (1.45/4.5/0.145), 1M context, mandatory reasoning with `max`/`high`/`low` efforts and no `off` level.
